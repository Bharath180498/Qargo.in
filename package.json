{
  "name": "porter-marketplace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run build --workspace @porter/shared && npm run start:dev --workspace @porter/backend",
    "build:backend": "npm run build --workspace @porter/shared && npm run build --workspace @porter/backend",
    "start:backend": "npm run start --workspace @porter/backend",
    "migrate:backend": "npm run prisma:migrate --workspace @porter/backend",
    "start:backend:migrate": "npm run migrate:backend && npm run start:backend",
    "dev:mobile": "npm run start --workspace @porter/mobile",
    "dev:mobile:fresh": "npm run start:fresh --workspace @porter/mobile",
    "dev:mobile:clear": "npm run start:clear --workspace @porter/mobile",
    "dev:mobile:tunnel": "npm run start:tunnel --workspace @porter/mobile",
    "dev:admin": "NEXT_TELEMETRY_DISABLED=1 NEXT_IGNORE_INCORRECT_LOCKFILE=1 npm run dev --workspace @porter/admin",
    "railway:configure-vars": "./infra/railway/scripts/configure_vars.sh",
    "railway:deploy": "./infra/railway/scripts/deploy_all.sh",
    "aws:build-push": "./infra/aws/scripts/build_and_push.sh",
    "aws:release": "./infra/aws/scripts/release.sh",
    "build": "npm run build --workspace @porter/shared && npm run build --workspace @porter/backend && npm run build --workspace @porter/admin && npm run build --workspace @porter/mobile",
    "lint": "npm run lint --workspace @porter/shared && npm run lint --workspace @porter/backend && npm run lint --workspace @porter/admin && npm run lint --workspace @porter/mobile",
    "test": "npm run test --workspace @porter/shared && npm run test --workspace @porter/backend && npm run test --workspace @porter/admin && npm run test --workspace @porter/mobile",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,yml}\""
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
